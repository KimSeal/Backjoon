#include <bits/stdc++.h>
using namespace std;
int main(){
    string str_1, str_2;
    cin >> str_1 >> str_2;
    if(str_1.size() >= str_2.size()){
        cout <<"go";
    }
    else{cout << "no";}
}
