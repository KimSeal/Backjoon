#include<bits/stdc++.h>
using namespace std;
int main() {
	int a1, a2, b1, b2, c1, c2;

	cin >> a1 >> a2 >> b1 >> b2 >> c1 >> c2;
	int temp = (a1 - b1) * (b2 - c2) - (a2 - b2) * (b1 - c1);
	if (temp > 0) cout << 1;
	if (temp == 0) cout << 0;
	if (temp < 0) cout << -1;
}
