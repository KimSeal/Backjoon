#include <bits/stdc++.h>
using namespace std;
int arr[1001];
int main() {
    int C, K;
    cin >> C >> K;
    for (int i = 0; i < C; i++) {
        cin >> arr[i];
    }
    sort(arr, arr + C);
    cout << arr[C- K];
}
