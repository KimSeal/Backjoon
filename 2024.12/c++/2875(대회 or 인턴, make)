#include<bits/stdc++.h>
using namespace std;
int main() {
    int N, M, K;
    cin >> N >> M >> K;
    while (K > 0) {
        if (N > M * 2) N--;
        else M--;
        K--;
    }
    if (N >= M * 2) cout << M;
    else cout << N/2;
}
