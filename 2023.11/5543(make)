#include <bits/stdc++.h>
using namespace std;
int main(){
    int a, b, c;
    cin >> a;
    for(int i=0;i<2;i++){
        cin >> b;
        if(a > b) a = b;
    }
    cin >> b;
    cin >> c;
    if(b>c) b= c;
    cout << a+ b - 50;
}
