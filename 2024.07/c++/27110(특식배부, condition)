#include <bits/stdc++.h>
using namespace std;
int main(){
    int N, A, B, C;
    cin >> N;
    cin >> A >> B >> C;
    int result=0;
    result += (A < N)? A : N;
     result += (B < N)? B : N;
     result += (C < N)? C : N;
    cout << result;
}
