#include <bits/stdc++.h>
using namespace std;
int N, K;
int main(){
    cin >> N >> K;
    int result = 1;
    for(int i=0;i<K;i++){
        result *= (N-i);
        result /= (i+1);
    }
    cout << result;
}
