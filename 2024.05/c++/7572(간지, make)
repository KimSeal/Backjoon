#include<bits/stdc++.h>
using namespace std;
//초반에 대한 예외처리가 중요하다.
int main() {
    char a[12] = { 'I','J', 'K', 'L', 'A', 'B', 'C','D', 'E', 'F','G', 'H' };
    int b[10] = { 6, 7, 8, 9, 0, 1, 2, 3, 4, 5 };
    int N;
    cin >> N;
    cout << a[N % 12] << b[N%10];
}
