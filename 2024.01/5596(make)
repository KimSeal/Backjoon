#include <bits/stdc++.h>
using namespace std;
int S, M;
int main(){
    int temp;
    for(int i=0;i<4;i++){
        cin >> temp;
        S+=temp;
    }
    for(int i=0;i<4;i++){
        cin >> temp;
        M+=temp;
    }
    temp = (S>=M) ? S:M;
    cout << temp;
}
