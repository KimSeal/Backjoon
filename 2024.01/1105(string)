#include <bits/stdc++.h>
using namespace std;
string L, R;
int result = 0;

int main(){
    cin >> L >> R;
	if(L.size() != R.size()) {cout << 0; return 0;}
	
    for(int i=0;i<L.size();i++){
		if(L[i]=='8' && R[i]=='8'){
			result++;
		}
		if(L[i] != R[i])break;
	}
	cout << result;
}
