#include<stdio.h>
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int A, B, C;
	scanf("%d %d %d",&A,&B,&C);
	int Num;
	if(B>=C){
		Num=-1;
	}
	else{
		Num=(A/(C-B))+1;
	}
	printf("%d",Num);
	return 0;
}
