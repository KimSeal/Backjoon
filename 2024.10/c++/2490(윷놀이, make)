#include <bits/stdc++.h>
using namespace std;
int main() {
    int a, b, c, d;
    char tempCh;
    for (int i = 0; i < 3; i++) {
        cin >> a >> b >> c >> d;
        a = a + b + c + d;
        tempCh = 'D' - a;
        if (a == 4) cout << "E\n";
        else cout << tempCh << "\n";
    }
}
