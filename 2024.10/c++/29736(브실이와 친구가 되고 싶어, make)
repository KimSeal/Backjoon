#include <bits/stdc++.h>
using namespace std;
int main() {
    int A, B, C, D;
    cin >> A >> B >> C >> D;
    A = (A > C - D) ? A : C - D;
    B = (B < C + D) ? B : C + D;
    if (A > C+D || B < C-D) cout << "IMPOSSIBLE";
    else cout << B - A + 1;
}
