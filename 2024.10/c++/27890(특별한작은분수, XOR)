#include <bits/stdc++.h>
using namespace std;
int main() {
    int A, B;
    cin >> A >> B;
    for (int i = 0; i < B; i++) {
        if (A % 2 == 0) {
            A = (A / 2) ^ 6;
        }
        else A = (2 * A) ^ 6;
    }
    cout << A;
}
