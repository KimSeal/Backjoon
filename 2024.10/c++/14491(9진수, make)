#include <bits/stdc++.h>

using namespace std;

int main() {
    int N, M;
    string str = "";
    cin >> N;
    while (N>0) {
        M = N % 9;
        N /= 9;
        str += M + '0';
    }
    for (int i = str.size()-1; i >= 0;i--) {
        cout << str[i];
    }
}
