#include<stdio.h>
int main(){
	int x0,y0,x1,y1;
	int dx,dy;
	int min;
	scanf("%d %d %d %d",&x0,&y0,&x1,&y1);
	dx=x1-x0;
	dy=y1-y0;
	min = (x0>dx) ? dx : x0;
	if(min>y0){
		min=y0;
	}
	if(min>dy){
		min=dy;
	}
	printf("%d",min);
	return 0;
}
