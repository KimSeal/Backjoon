#include<bits/stdc++.h>
using namespace std;

int main() {
	string str;
	cin >> str;
	for (int i = 0; i < str.size();i++) {
		if (i >= str.size() - 1 - i) {
			cout << "true";
			break;
		}
		if (str[i] != str[str.size() - 1 - i]) {
			cout << "false";
			break;
		}
	}
}
