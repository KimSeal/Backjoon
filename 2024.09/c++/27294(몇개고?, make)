#include <bits/stdc++.h>
using namespace std;
int main() {
    int a, b;
    cin >> a >> b;
    if (b == 0 && a > 11 && a < 17) {
        cout << "320";
    }
    else {
        cout << "280";
    }
}
