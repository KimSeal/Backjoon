#include <bits/stdc++.h>
using namespace std;
int main() {
    int N;
    char c;
    string str;
    cin >> N >> str;
    for (int i = 0; i < N; i++) {
        if (str[i] == 'I') cout << "i";
        else cout << "L";
    }
}
